import { TotalCartPipe } from './total-cart-pipe';

describe('TotalCartPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCartPipe();
    expect(pipe).toBeTruthy();
  });
});
